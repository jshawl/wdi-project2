<%= form_for @event||Event.new do |f| %>
  <%= f.text_field :title, placeholder:"What's going on?" %>

  <%= f.label :when, "Where:" %>
  <%= f.text_field :location_id, id:"location-field" %>

  <%= f.label :when, "When:" %>
  <%= f.time_field :when %>
  <%= f.submit %>
<% end %>
